[project]
name = "isc-threat-feeds"
dynamic = ["version"]
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.2",
    "gunicorn>=23.0.0",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "ruff>=0.13.2",
    "ty>=0.0.1a25",
]
